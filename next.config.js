/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    title: "Barbara Zarzycka",
    description: "Marketing Executive Account Developer",
  },
  images: {
    domains: ["cdn.sanity.io"],
  },
};

module.exports = nextConfig;
